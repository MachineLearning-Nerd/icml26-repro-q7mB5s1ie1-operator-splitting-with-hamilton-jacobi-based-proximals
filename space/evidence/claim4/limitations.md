# Limitations and deviations

The source proof intends a nonempty fixed-point set inherited from its general
PDHG lemma. If that is considered implicit, the result is best read as a
machine-checked missing-hypothesis diagnosis. The paper-scale sibling tests the
corrected, nonempty-minimizer interpretation.
