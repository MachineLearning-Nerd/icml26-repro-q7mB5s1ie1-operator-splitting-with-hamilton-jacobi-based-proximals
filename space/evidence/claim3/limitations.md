# Limitations and deviations

The counterexample targets the exact printed hypotheses. If a nonempty solution
set is treated as implicit from Theorem 3.1, it diagnoses an omitted assumption
rather than the intended corrected theorem. A separate branch directly runs
DRS and DYS with a nonempty solution set and a nonzero smooth `h`.
