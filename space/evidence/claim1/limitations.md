# Limitations and deviations

This is a transparent specialized proof-certificate checker, not a general
proof assistant kernel. The mathematical argument is independently
reconstructed and each consequential inference is explicit, but trusted rules
such as Gibbs integration by parts and dominated convergence are reviewed in
the source audit rather than derived from foundational logic.

No finite grid is described as proving the universal theorem.
